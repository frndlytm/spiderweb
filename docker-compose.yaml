version: "3.9"

services:
  app:
    build: "."
    ports:
      - "8000:5000"
    volumes:
      - ".:/"
    environment:
      APP_MODULE: "main:api"
      LOG_LEVEL: "info"

  redis-stack:
    image: "redis/redis-stack:latest"
    ports:
      - "6379:6379"
      - "8001:8001"
